import Component from "../component";
import './garage.scss'

const garage = new Component({
  selector: `.page__main`,
  template: `<div class="parallax-bg garage-parallax"></div>
            <div class="main-acc-container garage-container">
            </div>

`
});

export default garage;
